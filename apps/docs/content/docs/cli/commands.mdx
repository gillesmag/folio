---
title: Command help
description: Arguments and options generated from the folio executable.
---

{/* Generated by scripts/generate-cli.mjs. Do not edit by hand. */}

Regenerate this page from the repository with `pnpm --filter @folio/docs generate:cli`.

## folio

```text
Push, read and comment on markdown documents in Folio

Usage: folio [OPTIONS] <COMMAND>

Commands:
  login        Sign in through the browser (device flow) and store the session token
  logout       Forget the stored session token
  whoami       Show the signed-in user
  push         Push a markdown file (or stdin with "-") as a new document, or replace one with --id
  list         List your documents
  pull         Print a document's markdown source (or write it to a file)
  open         Open a document in the browser
  comments     List comments on a document
  rm           Delete a document
  completions  Generate shell completions
  help         Print this message or the help of the given subcommand(s)

Options:
      --server <SERVER>  Folio server URL (defaults to the configured one) [env: FOLIO_SERVER=]
      --json             Print machine-readable JSON instead of text
  -h, --help             Print help
  -V, --version          Print version
```

## folio login

```text
Sign in through the browser (device flow) and store the session token

Usage: folio login [OPTIONS]

Options:
      --server <SERVER>  Folio server URL (defaults to the configured one) [env: FOLIO_SERVER=]
      --json             Print machine-readable JSON instead of text
  -h, --help             Print help
```

## folio logout

```text
Forget the stored session token

Usage: folio logout [OPTIONS]

Options:
      --server <SERVER>  Folio server URL (defaults to the configured one) [env: FOLIO_SERVER=]
      --json             Print machine-readable JSON instead of text
  -h, --help             Print help
```

## folio whoami

```text
Show the signed-in user

Usage: folio whoami [OPTIONS]

Options:
      --server <SERVER>  Folio server URL (defaults to the configured one) [env: FOLIO_SERVER=]
      --json             Print machine-readable JSON instead of text
  -h, --help             Print help
```

## folio push

```text
Push a markdown file (or stdin with "-") as a new document, or replace one with --id

Usage: folio push [OPTIONS] <FILE>

Arguments:
  <FILE>  Markdown file to push, or "-" for stdin

Options:
      --server <SERVER>          Folio server URL (defaults to the configured one) [env: FOLIO_SERVER=]
      --title <TITLE>            Title; defaults to frontmatter title or the first heading
      --json                     Print machine-readable JSON instead of text
      --visibility <VISIBILITY>  Who can see the document [possible values: private, unlisted, public]
      --id <ID>                  Replace an existing document instead of creating one
  -h, --help                     Print help
```

## folio list

```text
List your documents

Usage: folio list [OPTIONS]

Options:
      --server <SERVER>  Folio server URL (defaults to the configured one) [env: FOLIO_SERVER=]
      --json             Print machine-readable JSON instead of text
  -h, --help             Print help
```

## folio pull

```text
Print a document's markdown source (or write it to a file)

Usage: folio pull [OPTIONS] <ID>

Arguments:
  <ID>

Options:
  -o, --output <OUTPUT>  Write to this path instead of stdout
      --server <SERVER>  Folio server URL (defaults to the configured one) [env: FOLIO_SERVER=]
      --json             Print machine-readable JSON instead of text
  -h, --help             Print help
```

## folio open

```text
Open a document in the browser

Usage: folio open [OPTIONS] <ID>

Arguments:
  <ID>

Options:
      --server <SERVER>  Folio server URL (defaults to the configured one) [env: FOLIO_SERVER=]
      --json             Print machine-readable JSON instead of text
  -h, --help             Print help
```

## folio comments

```text
List comments on a document

Usage: folio comments [OPTIONS] <ID>

Arguments:
  <ID>

Options:
      --server <SERVER>  Folio server URL (defaults to the configured one) [env: FOLIO_SERVER=]
      --json             Print machine-readable JSON instead of text
  -h, --help             Print help
```

## folio rm

```text
Delete a document

Usage: folio rm [OPTIONS] <ID>

Arguments:
  <ID>

Options:
      --server <SERVER>  Folio server URL (defaults to the configured one) [env: FOLIO_SERVER=]
      --json             Print machine-readable JSON instead of text
  -h, --help             Print help
```

## folio completions

```text
Generate shell completions

Usage: folio completions [OPTIONS] <SHELL>

Arguments:
  <SHELL>  [possible values: bash, elvish, fish, powershell, zsh]

Options:
      --server <SERVER>  Folio server URL (defaults to the configured one) [env: FOLIO_SERVER=]
      --json             Print machine-readable JSON instead of text
  -h, --help             Print help
```
